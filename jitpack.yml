jdk:
  - openjdk17

before_install:
  - yes | sdkmanager --licenses || true
  - sdkmanager "platforms;android-36" "build-tools;36.0.0" "platform-tools" || true

install:
  - ./gradlew -PskipSigning=true :library:assembleRelease -x lint -x test